# 2026-07-16

## 同步 upstream + 移植补丁
- 同步 `origin` (hzleihuan) 到 `d31296a` = CodeBuddy 4.10.1 (build 33158423, hash 3ad58bcb)。
- 添加 `upstream` 远程 = `JipZeonGit/codebuddy-ide-cn-linux` (参考上游)。`upstream` 也发布 4.10.1 (tag `6928c99` = refactor: 全面基于官方 Linux DEB 提取)；其 `main` HEAD `4f11594` 在 4.10.1 tag 之后又加了 conda npm 修复 + CI/CD 测试工作流。
- 从 upstream 移植三处补丁 (cherry-pick 风格，未整体 merge，因两条线架构不同)：
  1. `scripts/lib/native-modules.sh` 两处 conda 环境 npm 变量清理 (`npm_config_*`/`NPM_CONFIG_*`)，防 `ERR_INVALID_URL`（upstream 4f11594）。
  2. 新增 `.github/workflows/ci-test.yml` 跨发行版构建/安装 CI（debian/fedora/arch/appimage），硬编码仓库路径已改为 `codebuddy-ide-cn-loong64`。
  3. `.github/workflows/aur-publish.yml` 末尾追加 `test-aur-install` job（对应 upstream 4f11594 的 +26 行增量）。
- 验证：`make check` 通过。改动 **未提交**（待用户确认是否 commit/push）。
- 未移植：upstream `install.sh`/`deb.sh`/`electron.sh` 等上千行结构差异（其把 electron.sh 合并进 deb.sh 的重构路线），直接 merge 会冲突并回退版本配置到 4.9.15 时代。
- 已 commit 并 push：`57273a3` → `origin/main` (hzleihuan)。3 files changed, 232 insertions(+)。

## Rebase on upstream/main with loong64 changes
- 用户要求以 upstream 为基础 + loong64 架构支持。
- 执行 `git reset --hard upstream/main`（4f11594），然后施加 13 个文件改动（11 修改 + 2 新增）：
  - **新增**: `scripts/lib/electron.sh`（loong64 Electron 下载/镜像），`CODEBUDDY.md`（项目指南）
  - **install.sh**: 引入 electron.sh，存 loong64 Electron 替换 x64，启动器改用 electron 二进制，仅复制 resources/app
  - **native-modules.sh**: 强制全部重建（移除"已有 .node 就跳过"逻辑，因为 .deb 中 x64 .node 无法在 loong64 运行）
  - **appimage.sh**: 架构感知版（uname -m），AppRun 调用 start.sh
  - **build-{deb,rpm,pacman}.sh**: ARCH 硬编码 loong64/loongarch64
  - **install-deps.sh**: NodeSource 白名单加 loong64，所有发行版加 unzip
  - **Makefile**: loongarch64 AppImage 配置，修正过时的帮助文本
  - **README.md**: loong64 专用文档
  - **.aur/PKGBUILD**: loongarch64，指向 hzleihuan 仓库
- 语法检查全部通过（bash -n）。
- 保留的 upstream 改进：ar+tar deb 提取、remove_known_wrong_platform_modules、purge_macho_native_modules、ImageMagick 图标缩放、桌面文件提取、CI/CD 工作流。
- **尚未提交/推送**（待用户确认）。
